# Samples

Place small, legally shareable firmware samples here for manual experiments.

The automated integration tests build a tiny tar-based Linux-like root filesystem
on the fly and do not require checked-in firmware blobs.

